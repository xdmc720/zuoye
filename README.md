# 🚀 Vue 导航站

基于 **Vue 3 + Vite + Vue Router** 构建的个人导航网站。

> 📌 本项目是在原有的 [skfoa 导航站](https://github.com/skfoa/cf-worker-nav)（基于 Cloudflare Worker + 原生 JavaScript）基础上，使用 Vue 框架重构而成，用于学习和演示 Vue Router 路由功能。

## ✨ 功能特性

- 🏠 **多页面路由** - 首页、分类页、关于页、设置页
- 🔍 **多搜索引擎** - Google、百度、Bing、GitHub、站内搜索
- 🎨 **主题切换** - 支持深色/浅色模式
- 📱 **响应式设计** - 完美适配移动端
- 💎 **精美 UI** - 毛玻璃效果、流畅动画

## 📁 项目结构

```
src/
├── main.js              # 应用入口
├── App.vue              # 根组件
├── router/
│   └── index.js         # 路由配置
├── data/
│   └── links.js         # 导航数据
├── views/
│   ├── HomeView.vue     # 首页
│   ├── CategoryView.vue # 分类页
│   ├── AboutView.vue    # 关于页
│   └── SettingsView.vue # 设置页
└── assets/
    └── main.css         # 全局样式
```

## 🛠️ 技术栈

| 技术 | 说明 |
|------|------|
| Vue 3 | 前端框架 |
| Vite | 构建工具 |
| Vue Router | 路由管理 |
| CSS3 | 样式（变量、Grid、Flexbox） |
| Cloudflare Pages | 部署托管 |

## 🚀 快速开始

```bash
# 安装依赖
npm install

# 开发模式
npm run dev

# 构建生产版本
npm run build
```

## 📖 Vue 特性演示

本项目演示了以下 Vue 核心特性：

| 特性 | 文件 | 说明 |
|------|------|------|
| Vue Router | `router/index.js` | 路由配置与导航 |
| 动态路由 | `CategoryView.vue` | `$route.params` 获取参数 |
| 组件通信 | `SettingsView.vue` | props / $emit |
| 计算属性 | `HomeView.vue` | computed 过滤数据 |
| 生命周期 | `App.vue` | mounted 钩子 |
| 条件渲染 | `HomeView.vue` | v-if / v-else |
| 列表渲染 | `HomeView.vue` | v-for 循环 |
| 事件绑定 | `App.vue` | @click 事件 |
| 双向绑定 | `HomeView.vue` | v-model 输入框 |

## 📝 License

MIT
